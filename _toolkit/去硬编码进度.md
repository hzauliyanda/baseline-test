# 去硬编码 → 配置化 进度（productization 第一步）

目标：把环境/租户/平台/路径等实例特有项从代码里抽走，改成读 `config.yaml`，让框架能给别人用。

## ✅ 已完成（_toolkit，2026-08-02）

- **`config.yaml`**（真实值，已 gitignore）+ **`config.example.yaml`**（脱敏模板，提交）+ **`config.py`**（加载器，`root` 自动推导、`abspath()`、token 走环境变量）。
- **`upload_cases.py` / `move_cases.py`**：删掉写死的 `FLOWS` 绝对路径表 + octopuses 平台参数(TOKEN/SPACE_ID/GROUP_ID/PID/ENDPOINT) + 租户默认(appId/cookie)，全改读 config。验证：6 条 flow 全解析到真实文件、`/Users/` 字面量清零。
- **`cdp_type.py`**：写死的 `/Users/liyanda/.claude/...` skill 路径 → `~/.claude/...` + 环境变量 `API_FLOW_RECORDER_SCRIPTS` 可覆盖。
- **`.gitignore`**：config.yaml(含token)、.chrome-profile、.chrome-port、capture 不入库。

## ⬜ 待做（skill 包 api-flow-recorder，属②配置驱动阶段）

> 这些在共享 skill 里，已用 `~`/`$HOME`（**跨用户可移植**），但根位置/Chrome 路径仍写死；发布 skill 包时收口。

| 文件:行 | 硬编码 | 建议 |
|---|---|---|
| `launch_chrome.sh:9` | `CHROME=/Applications/Google Chrome.app/...`（仅 macOS） | 读 config.chrome.binary + OS 探测 |
| `cdplib.py:13`、`tail_api.py:13`、`to_curl.py:14`、`recorder.py` | `ROOT=~/AI-TEST/api-flows` 位置写死 | 读 config 或环境变量 `API_FLOWS_ROOT` |
| `cdp.py:146` | 默认截图路径写死 | 同上 |
| `cdp_type.py:5`（skill 内那份） | `/Users/liyanda/.claude/...` | 同 _toolkit 已修法：expanduser + 环境变量 |
| 各 flow 的 `variables` | appId=4/configId=21/cookie 等租户值 | 已是 `{{占位}}`/`{{ENV:}}`，属实例配置，保持 |

## 下一步（可选）
- 把上表 skill 包的 5 处也收口 → 进入②「配置驱动工具箱」。
- `doctor` 命令：查依赖 + 校验 config + 探测 Chrome，降低"在我机器上能跑"门槛。
