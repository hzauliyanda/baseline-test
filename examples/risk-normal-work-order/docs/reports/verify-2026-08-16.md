# 回归审查 verdict（verify-2026-08-16）
- 模块：risk-normal-work-order ｜ 审查者：干净上下文（Claude Code subagent，只读审查）
- 结论：**FAIL**
- 机算 recon：docs/reports/verify-recon-2026-08-16.json（HARD_FAIL）
- 说明：本文件同日早前承载「ego 迁移轮」verdict（基于 14:4x 产物，20 PASS/2 SKIP），该轮产物已被
  17:04–17:13 全量重跑取代；按本次审查指示以本轮 verdict 覆盖。

## 五项逐项
1. 数字对账：✅ API 138 steps / 136 PASS / 2 FAIL / 46 db_check、UI 场景级 22=21 PASS+1 FAIL+0 SKIP、
   step 级 49=48+1——亲手重算与 recon checks[B].recomputed 完全一致；总览 HTML 抽 7 处数字
   （136/138/21/22/1 FAIL/2 FAIL/46 🟡DB）全对；时间链 17:04(API)→17:11:51(UI JSON 落盘+HTML 同秒)
   →17:13:27(S13 复跑后恢复备份) 自洽，`started` 字段经查 runner 源码（save_results）实为落盘时刻，非伪造痕迹。
2. 三桶纪律：✅（小注）seed=20260816 抽中 flow.yaml(9步) / flow-supplement.yaml(23步) /
   flow-negative.yaml(50步)：🟢 全部接口层可判（status / $.code equals / $.message contains / exists /
   equals），无文案·时序·跨系统副作用混入；判不动的 18 步为空 assert + skip_note🔴（9 步带 ⏳待回填，
   标记诚实）；抽中文件的全部 17 个 🔴 均在人工校验清单有条目。db_check 表名（t_cs_issue /
   t_cs_issue_config / t_cs_notify_config / t_cs_issue_field_value）模块内自洽，t_cs_issue_config 有
   缺口分析-2026-08-15 旁证；但模块内无权威 schema 锚点（audit_base 未回填）——🟡 本就人工执行，风险可控。
3. 断言真实性：✅ seed=20260816 抽 5 条 PASS：neg_cfg_name_exist#status、draft_cleanup#$.code=SUCCESS、
   neg_cfg_find_id#status、config_refind_id#exists、enums#exists——actual 均与 resp_preview 互证，
   无恒真断言；2 条 FAIL 断言为真实期望落空（INVALID_ARGUMENT↔SUCCESS、contains required↔空串），
   如实落盘未抹红；✅/⏳ 标记诚实（⏳ 留在 note、assert 留空，未伪装已验证）。
   小注：supplement/negative 的 issueConfigName 落库值含未替换的字面量 `{{run_id}}`，
   人工执行 db_check SQL 时须照抄字面量。
4. 覆盖对账：❌ check_coverage.py 亲手复跑 EXIT=1（与 recon 一致，7 缺口）；加 --check-case-ids 为
   **8 缺口**（多出：矩阵引用 glob id `all-types:*_approve` / `all-types:*_wo_save` 不存在）。
   seed=20260816 抽 2 枚举行：ui[模板类型(创建弹窗走查)] OTHER→S2、NAME_LIST_APPLY→S16 ✅ 真实存在；
   api[工单状态] flow:create/approve、paths:draft_create / multi_approve_step1/2、all-types 各字面 id
   ✅ 全部存在；但 **'3已驳回'→"flow-negatives 相关 reject 步" 是悬空散文引用（全部 flow*.yaml 无任何
   reject 步）＝第二种假覆盖（列了号但用例不存在）**——审查员独立新发现，recon 未报。
5. SKIP/FAIL 理由：本轮 0 SKIP；口径「S4/S5 条件 SKIP」已过期（本轮 S4/S5 实跑且 PASS，17:06 截图为证），
   漂移方向无害但口径须更新。API 2 FAIL 与口径恒红 id 精确一致，actuals（SUCCESS/空串）佐证「服务端
   校验缺口」定性。UI S13 FAIL detail 原文即「ego CLI 挂死(>20s)」通道错误、非断言不匹配；复跑取证
   s13-flake-rerun-2026-08-16.md 判「通道 flake」——**采纳但降置信**：旁证充分（17:13 新鲜截图
   S13-ego-01-审批上限.png 与场景定义吻合、step1 在代码上本就无截图、全量 JSON 诚实保留 FAIL、
   备份→恢复时间链自洽），但复跑原始 JSON/日志未落盘，disabled=True 与 toast×3 系利害方自述，不可复核。

## recon findings 裁决
- [C] S13 FAIL 口径未容纳：**成立**。flake 定性采信（单场景复跑+落盘旁证，满足「不许口头宣布」底线），
  但本轮落盘证据 21 绿/1 红与口径「22 场景全绿」不符，口径未更新前不得宣称达标 → 更新 baseline.yaml
  口径或重跑全量。
- [D] check_coverage 不放行：**成立**（复跑 EXIT=1，7 缺口；--check-case-ids 下 8 缺口）→ 补 10已废弃、
  状态机 3 状态、UI 枚举 3 值、endpoint 1 个、回填 audit_base×2、清 glob 引用后重跑放行门。
- [E] api-coverage covered['10已废弃'] 空=假覆盖：**成立**（该值同时在 gaps 如实申报，属冗余申报而非
  隐瞒，但按 kit 语义即假覆盖行）→ 补用例或删空键并挪 gaps/exempt。
- warnings（audit_base backend/frontend 待回填）：属实，维持 warning（②tc-cases 前置门会因它 FAIL）。
- 新增（审查员）：'3已驳回' 悬空散文引用＝第二种假覆盖，须替换为真实 step id 或挪 gaps。

## 结论依据（≤5 行）
- 数字、三桶、断言真实性三项干净，无伪造迹象。
- 但 PROMPT FAIL 条款「假覆盖」明确命中两条（10已废弃 空行 + 3已驳回 悬空引用），覆盖门 EXIT=1 不放行。
- S13 新红 flake 判定证据基本充分但不可完全复核，且口径未更新容纳。
- 三者任一即 FAIL → **FAIL**；修复覆盖矩阵与口径后重跑 tc-verify 再收口。
