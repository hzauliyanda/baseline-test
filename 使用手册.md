# api-flows 使用手册（给采用者）

> 一套把「录制回放」升级成「**可信回归**」的测试基线工具。本手册讲**怎么装、怎么配、怎么跑起来**。
> 方法论细节（三桶模型/两段式断言/信任层）见同目录 `README.md`（playbook）。

---

## 目录
1. [这是什么 / 能帮你做什么](#1-这是什么--能帮你做什么)
2. [前置要求（先确认）](#2-前置要求先确认)
3. [安装](#3-安装)
4. [首次配置](#4-首次配置)
5. [快速上手：建你的第一个模块](#5-快速上手建你的第一个模块)
6. [核心概念（3 分钟看懂为什么可信）](#6-核心概念3-分钟看懂为什么可信)
7. [日常怎么跑回归](#7-日常怎么跑回归)
8. [目录结构说明](#8-目录结构说明)
9. [常见问题 & 坑](#9-常见问题--坑)
10. [名词表 & 参考](#10-名词表--参考)

---

## 1. 这是什么 / 能帮你做什么

给一个**系统 URL + 模块**，帮你产出并维护一套回归基线：
- **功能地图**（模块所有页面/功能/接口的完整清单，一切用例的源）
- **接口用例 + UI 用例**（可自动跑）
- **可信断言**：接口返回 SUCCESS ≠ 数据对——本工具用「两段式断言 + 三桶模型」逼你把"真的成功了吗"验到底，而不是让机器自欺。

一句话：**不是让你信任工具/AI，而是让结果由机器判定 / SQL / 人眼决定。**

## 2. 前置要求（先确认）

| 项 | 说明 |
|---|---|
| **Claude Code** | ⚠️本工具的"引擎"是 Claude Code agent 照着 skill 跑。**你得是 Claude Code 用户**，大部分步骤是"在 Claude Code 里让它调某个 skill"，不是敲原始命令。 |
| 操作系统 | 目前脚本默认 **macOS**（Chrome 路径写死）。Linux/Windows 需改 `config.chrome.binary`，且 skill 包内 `launch_chrome.sh` 也要同步改（见 `_toolkit/去硬编码进度.md`）。 |
| Python3 | + `pyyaml`、`requests`、`websocket-client`。 |
| Chrome | 桌面版 Chrome（探索用持久化 profile 驱动）。 |
| 测试环境访问 | 被测系统的**测试环境** URL + 一个能登录的账号。 |
| 后端代码仓库 | 做"接口↔代码分支覆盖审计"需要（可选但强烈建议）。 |
| DB 只读权限 | 🟡 桶的"数据真伪"校验要跑 SQL；没有就只做到 🟢 桶，🟡 标待人工。 |

## 3. 安装

**① 装 skills**（进你的 `~/.claude/skills/`）：`api-flow-recorder`、`p2-test-case-generator`、`p3-ui-test-case-generator`、`p4-browser-test-runner-devtools`、`ai-web-test-pipeline`。
> 组内有 skill 分发就走分发；否则从本仓库的 skills 目录拷进 `~/.claude/skills/`。

**② 拿工具箱**：把本仓库 clone/拷到本地（含 `_toolkit/`、`config.example.yaml`、`README.md`、`new-module.sh`）。默认放 `~/baseline-test`（2026-08-16 起统一此路径，原 `~/AI-TEST/api-flows` 已迁并）。

**③ 装依赖**：
```bash
python3 -m pip install pyyaml requests websocket-client
```

## 4. 首次配置

```bash
cd ~/baseline-test
cp config.example.yaml config.yaml     # config.yaml 已被 gitignore，含密钥不入库
```
编辑 `config.yaml`：
- `chrome.binary`：你 OS 的 Chrome 路径。
- `systems.<你的系统>`：`base_url`（测试环境）、`headers`（租户 appId 等）、`cookie_env`（cookie 用哪个环境变量）。
- `octopuses`：若用测试平台上传用例才填；不用可留空/删。
- `uploads`：要上传的 flow 清单（label + 相对路径）。

**敏感值走环境变量**（不写进文件）：
```bash
export <系统>_COOKIE='整段cookie'      # 如 export RISK_COOKIE='...'
export OCTOPUSES_TOKEN='xxx'           # 若用测试平台
```
自检：
```bash
python3 _toolkit/config.py             # 打印 root/systems/uploads，能跑通即配置 OK
```

## 5. 快速上手：建你的第一个模块

```bash
# ① 建骨架（auto/docs 布局 + _meta.yaml + 清单模板）
bash _toolkit/new-module.sh <系统> <模块>       # 例：risk warning-handle

# ② 填 <系统>/<模块>/_meta.yaml：env_url、module_base_path、repos.backend
```

③ 之后在 **Claude Code 里**逐步让它做（对话驱动）：
1. **探索** —— "用 api-flow-recorder 探索 `<系统>/<模块>`"→ 产出 功能地图.md + `auto/api/flow.yaml`。首次会弹 Chrome 让你**手动登录**（登录态存持久化 profile，之后免登）。
2. **出用例** —— p2 生成功能用例、p3 生成 UI 用例。（想一键串：`ai-web-test-pipeline`）
3. **冲烟自测** —— 跑一遍验用例可用 + 回填断言里的 `⏳`（**早跑，不出正式报告**）。
4. **后端信任层** —— 让它做"接口↔后端代码分支覆盖审计"→ 据空白补 negative/边界用例 + **回填功能地图**；每条 case 上**两段式断言**；生成**人工校验清单**。
5. **【里程碑① 正式回归】** —— 用例补全后跑一次，出正式报告；三桶都过才算真过。
6. **（后续）前端线** —— git 前端代码后填 `repos.frontend`，补 UI 场景 + 前端覆盖审计【里程碑②】。

> 详细每步的产物/规范见 `README.md` 的 Playbook 章节。

## 6. 核心概念（3 分钟看懂为什么可信）

**三桶信任模型** —— 每条 case 的结果由谁判：
- 🟢 **机器判定**：接口层确定性判据（错误码/状态码/回读），runner 自动评分。
- 🟡 **DB 兜底**：接口 SUCCESS ≠ 数据对，真伪由 **SQL 人工核**。
- 🔴 **纯人工**：跨系统/越权/并发/UI 视觉，机器做不了。

**两段式断言**（写在 flow yaml 每步）：
- `assert:` 🟢 runner 唯一评分（只放能判死的）
- `db_check:` 🟡 真表名 SQL，**runner 忽略→永不自动通过**（逼你人工核数据）
- `skip_note:` 🔴 runner 跑不了的
- 出处标 `✅实跑确认` / `⏳待回填`——**读代码推断的值绝不写成硬断言**。

**功能地图 = 唯一、活的、完整真源**：发现新逻辑（含代码审计）**必须回填功能地图**，最终它覆盖所有场景。

## 7. 日常怎么跑回归

```bash
export <系统>_COOKIE='...'      # 先注入登录态
# 🟢 接口自动回归（执行器在 regression-runner skill 包里，已合并原 runner.py）
python3 ~/.claude/skills/regression-runner/scripts/regression_runner.py <系统>/<模块>/auto/api/flow.yaml --smoke
#   --dry-run 只看渲染后的请求；--only <stepid> 单跑；--stop 失败即停；--set k=v 补前置值；去掉 --smoke 出 HTML 报告
```
- 执行后、闭环沉淀前：**verify⑩执行审查**（`~/.claude/skills/verify/`）——跑完回归必派
  只读 subagent 对账汇报与产物，verdict 落盘 `docs/reports/verify-<日期>.md`；
  无 verdict 或 FAIL 的轮次不得宣称完成、不得进入 baseline-sync。
然后照 `docs/checklists/人工校验清单.md`：
- 🟡 用执行输出里的 id 替换 SQL 的 `<...>`，在有权限的 DB 逐条跑，比对期望行。
- 🔴 按 Tier 人工执行/勾选。
- **三桶都过 = 这轮真的过。**

## 8. 目录结构说明

```
<系统>/<模块>/
├── _meta.yaml            模块元信息 + 仓库指向（backend/frontend）
├── 功能地图.md           总源（含 action→api 映射）
├── auto/                 🤖 机器执行：api/(flow yaml)、ui/(runner)、screenshots/、_created.yaml
└── docs/                 👀 人读：functional-cases/、ui-cases/、checklists/、reports/
```
（`auto`=喂机器，`docs`=给人看。详见 README「模块内部规范」。）

## 9. 常见问题 & 坑

| 现象 | 处理 |
|---|---|
| 探索时登录态失效 | 在弹出的 Chrome 窗口手动重登，从断点续跑。 |
| 非 macOS 跑不起 Chrome | 改 `config.chrome.binary`；skill 包 `launch_chrome.sh` 的 Chrome 路径也要改（见 `_toolkit/去硬编码进度.md`）。 |
| 没有 DB 权限 | 只做到 🟢 桶；🟡 的 SQL 留在清单里，标"待有权限时人工核"。 |
| 工具箱换了目录 | skill 脚本当前默认 `~/baseline-test`；换位置需按去硬编码进度调整根路径。 |
| 想上传用例到测试平台 | 填 `config.octopuses` + `export OCTOPUSES_TOKEN`，跑 `python3 _toolkit/upload_cases.py`。 |
| "我不是 Claude Code 用户" | 当前版本不适用——引擎是 Claude Code agent。 |

## 10. 名词表 & 参考

- **flow / flow yaml**：接口用例（声明式，runner 执行）。
- **功能地图**：模块全场景清单，用例的源。
- **三桶 / 两段式断言**：见 §6。
- `README.md` —— 建基线的完整方法论 playbook。
- `config.example.yaml` —— 配置模板。
- `_toolkit/去硬编码进度.md` —— 可移植性现状 + 待收口清单。
- `new-module.sh` —— 建模块骨架脚手架。
