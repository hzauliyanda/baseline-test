# 普通工单 · 前后端 git 变更 vs 用例缺口分析（2026-08-15）

> 分析方式：**降级 L1（文件级）+ LLM 语义核对**。
> 原因：`~/AI-TEST/testcase-baseline` 基线仓库本机不存在 → impact-go CLI 不可用，无法做 L2 AST/调用图分析。文件级匹配 + 人工读 diff 补语义影响。

## 一、分析窗口

| 侧 | 仓库 | base → head | 说明 |
|---|---|---|---|
| 后端 | `~/Documents/project_code/go/src/armor-smart-platform`（feature/zym/monitor-0708，已 fetch） | `59d0e27da`（07-28，基线审计时点）→ `origin/master 81cf0e87a`（08-05） | 56 个变更文件 |
| 前端 | `~/Documents/project_code/frontend/risk-cooperation` | 全分支最新停在 **06-11** | UI 用例（08-04 产）之后无新前端代码进本仓 |

## 二、后端结论：窗口内 0 个普通工单变更

56 个变更文件 `grep -E "normal_issue|issue_config|cs/"` 命中 **0**。变更集中在其它模块：

- 特权账号（privilegeaccount）、威胁分析（threat）、监控告警（monitor/early_warning_task_template）、
  处罚事件（policy-event）、Kafka 消费、xxl-job、policy/feature（字段枚举 repo、正则 repo）

边界核查：本地分支顶部两提交 `fae1c7bc4`（模板引用查询）/`7784fe88b`（模板提供枚举）均为 **monitor 模块**（monitor_alarm_reference / monitor_template_controller），与 cs/issue 无关，且已在 07-28 基线内。

**语义影响判断**：变更的 `service_init.go`、`field_enum_repo.go`、`t_feature_regex_repo.go` 属 policy/feature 机制，供策略/特征侧消费；普通工单的字段模板走 `issue_config`（t_cs_issue_config）独立链路，未引用上述变更文件。**判断为无影响**（LLM 推断，非调用图实证——L2 恢复后可复核）。

## 三、前端结论：无增量可分析

前端仓所有分支停在 06-11，晚于 UI 用例产出（08-04）的前端变更不在本仓。**测试环境实际部署的前端可能比本仓新**——若页面行为与用例不符，以环境为准，此为本分析的精度缺口。

## 四、遗漏场景判定

- **git 视角：无新增遗漏**——窗口内后端无普通工单端点/逻辑变更，前端无新代码。
- **存量已知缺口**（08-02 代码分支覆盖审计产物，非本次新增）：入参空值/必填缺失/非数字 id/枚举非法/越权类负向，当时 ≈0 覆盖。**现已回填**：
  - flow-negative.yaml（50 步：空 issueId / 非数字 id / 非法 configId / 缺必填模板字段 / 越权 / 并发锁 / 终态重复审批…）
  - flow-supplement.yaml（23 步：无权限保存 / 通知配置 CRUD / 非最后审批人编辑…）
  - flow-paths.yaml（21 步：草稿流转 / 多级审批 / 品牌授权链）
  - flow-all-types.yaml（35 步：名单申请/剔除/其他×三类模板 × 建查审删全链）
  - flow.yaml（9 步主链，08-15 新增 edit_verify/approve_verify 回读断言）

## 五、后续

1. 本轮全流程回归（npm run regression）执行中，结果另行落报告 + verify⑩ 审查。
2. testcase-baseline 仓库缺失 → impact-go L2 不可用，建议恢复基线仓库后对本次「无影响」判断做调用图复核。
3. api_runner.py（npm 路径执行器）丢 query params 的 bug 未修——本轮若 fixed_recipient 类用例红，先查此处再定性。
